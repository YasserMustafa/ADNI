function model = evaluate_models(data, numStates, numFolds, varargin)
%% Function to train a HMM for varying k values and evaluate the best model
%
% data          -- data{i} is a d*seqLength(i) matrix of observations,
%                  where d = dimension of feature vector
% numStates     -- a vector containing the range of 'K' (# states)
%                  we consider

end